# Local definition 14: Brightness temperature

unsigned[4] channelNumberRaw = 0 : dump ;
alias mars.channel = channelNumberRaw;

when ( scaledValueOfCentralWaveNumber || satelliteNumber || instrumentType || satelliteSeries ) {
    set_nofail channelNumberRaw = channelNumber;
}
